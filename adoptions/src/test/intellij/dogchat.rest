# 3 endpointes, 2 different users: user, user2

### First statement
GET {{host}}/{{base_path}}/assistant?user={{user}}&question="my name is Måns!"

> {%
    client.test("Status", function () {
        client.assert(response.status === 200, "Unexpected response status");
    });
%}

### First question
GET {{host}}/{{base_path}}/assistant?user={{user}}&question="schedule a meeting with Devil!"

> {%
    client.test("Status", function () {
        client.assert(response.status === 200, "Unexpected response status");
    });
%}

### Followup question
GET {{host}}/{{base_path}}/assistant?user={{user}}&question="fantastic. when can i schedule an appointment to pickup Prancer from the New York City location?"

> {%
    client.test("Status", function () {
        client.assert(response.status === 200, "Unexpected response status");
    });
%}

### Fetch messages
GET {{host}}/{{base_path}}/messages?user={{user}}

> {%
    client.test("Status", function () {
        client.assert(response.status === 200, "Unexpected response status");
    });
%}

### Clear messages
DELETE {{host}}/{{base_path}}/messages/clear?user={{user}}

> {%
    client.test("Status", function () {
        client.assert(response.status === 204, "Unexpected response status");
    });
%}

### Dummy endpoint for security test without token
DELETE {{host}}/{{base_path}}/messages/dummy?user={{user}}

> {%
    client.test("Status", function () {
        client.assert(response.status === 401, "Unexpected response status");
    });
%}

### create tokens first, should then work, both read and write scope
DELETE {{host}}/{{base_path}}/messages/dummy?user={{user}}
Authorization: Bearer {{normal_access_token}}

> {%
    client.test("Status", function () {
        client.assert(response.status === 204, "Unexpected response status");
    });
%}

### create tokens first, should then work, both read and write scope
DELETE {{host}}/{{base_path}}/messages/dummy?user={{user}}
Authorization: Bearer {{long_access_token}}

> {%
    client.test("Status", function () {
        client.assert(response.status === 204, "Unexpected response status");
    });
%}

### create tokens first, should then work, only write scope
DELETE {{host}}/{{base_path}}/messages/dummy?user={{user}}
Authorization: Bearer {{write_access_token}}

> {%
    client.test("Status", function () {
        client.assert(response.status === 204, "Unexpected response status");
    });
%}

### Expired token, 401, setting clock skew to Zero, stops giving initial success
DELETE {{host}}/{{base_path}}/messages/dummy?user={{user}}
Authorization: Bearer {{short_access_token}}

> {%
    client.test("Status", function () {
        client.assert(response.status === 401, "Unexpected response status");
    });
%}

### Should not, scope=read, write is missing
DELETE {{host}}/{{base_path}}/messages/dummy?user={{user}}
Authorization: Bearer {{read_access_token}}

> {%
    client.test("Status", function () {
        client.assert(response.status === 403, "Unexpected response status");
    });
%}

### Info
GET {{host}}/actuator/info

### Check operations
GET {{host}}/actuator/metrics/gen_ai.client.operation

### Check token usage
GET {{host}}/actuator/metrics/gen_ai.client.token.usage

### Health
GET {{host}}/actuator/health

### Liveness
GET {{host}}/actuator/health/liveness

### Readiness
GET {{host}}/actuator/health/readiness
